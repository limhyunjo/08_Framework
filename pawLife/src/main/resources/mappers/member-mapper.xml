<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.project.pawlife.member.model.mapper.MemberMapper">
<!--  매퍼 인터페이스에서 메서드 호출 시 xml 파일에 같은 메서드명과 id를 가진 SQL이 수행됨-->



<!--  로그인 -->
<!-- select는 파라미터 타입을 안써도 된다 
/ 마이 바티스의 타입 핸들러가 대신 해줌
-> parameterType 속성은 필수가 아님 / resultType은 필수 임!
-->

<!-- 표기법이 카멜로 바뀌면서 멤버 필드명과 같아짐
알아서 저장되도록 멤버라는 곳에 저장
 멤버 별칭 등록 해놔서 패키지명 안써도 됨
 -->
<select id="login" resultType="Member">
	SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_NICKNAME, MEMBER_PW,
	         MEMBER_TEL , PROFILE_IMG, AUTHORITY ,
	         TO_CHAR(ENROLL_DATE, 
	         'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"' )ENROLL_DATE 
	FROM "MEMBER"
	WHERE MEMBER_EMAIL = #{memberEmail}
	AND    MEMBER_DEL_FL = 'N'
</select>

</mapper>
